# Warroom Iran Incident 页面实现待办

## 任务拆解
1. 初始化项目结构与依赖，建立 TypeScript + Next.js + Tailwind + Vitest 基础。
2. 落地数据模型（JSON Schema）与初始数据（seed + source registry）。
3. 实现数据层：校验、过滤、可信来源检查、内容安全检查。
4. 实现 API 路由（events/infrastructure/statements/factcheck/sources）。
5. 实现 UI 组件与页面信息架构（更新时间、筛选、时间线、地图点位、状态卡、声明对照、核查、媒体、FAQ）。
6. 实现测试：schema、API 过滤、UI 安全约束、E2E 占位。
7. 执行验证（测试 + 构建），记录结果。
8. 增加 API `lang=zh|en` 双语响应能力。
9. 增加实时消息获取（服务端轮询 + SSE + 前端实时面板）。
10. 扩展“可信来源”能力到社交媒体白名单账号，并将其单独展示在页面侧栏。

## 依赖关系
- 2 依赖 1。
- 3 依赖 2。
- 4 依赖 3。
- 5 依赖 2/3。
- 6 依赖 3/4/5。
- 7 依赖全部前置任务。
- 8 依赖 2/3/4。
- 9 依赖 8。
- 10 依赖 2/3/8/9。

## 验收标准
- Schema 校验通过，非法记录被拒绝。
- API 支持 from/to/region/category/min_confidence/verification_status/q 查询。
- UI 默认不自动播放媒体；未验证内容显示【未验证】。
- 每条信息可追溯至来源链接与发布时间。
- `npm test` 通过，`npm run build` 通过。
- `/api/*` 支持 `lang=zh|en`，同一条数据可返回双语文本。
- 页面能在 1 次打开后持续接收实时消息推送（SSE 心跳 + 更新事件）。
- 页面侧边栏新增“可信社媒来源监控”区，且仅展示 `sources.registry.json` 白名单域下的社媒条目。
- `querySocialMediaSources()` 与实时轮询路径包含社媒来源并在测试中有覆盖。

---

## 2026-02-28 地图信息密度提升（参考新闻地图样式）

### 任务拆解
1. 重构 `MapPins`：支持伊朗主视图与区域外溢视图切换。
2. 将点位样式调整为红色小方点，并提供城市/事件短标签标注。
3. 保留地图与时间线联动（点击点位仍选中事件）。
4. 更新地图容器与图例样式（灰阶底图、高密度信息排布）。
5. 在主页面传入 `regional_impacts` 并展示区域外溢地图信息。
6. 执行回归验证：`npm test`、`npm run build`。

### 依赖关系
- 2/3 依赖 1。
- 4 依赖 1/2。
- 5 依赖 1。
- 6 依赖全部前置任务。

### 验收标准
- 地图默认显示伊朗视图，能看到红色方点与地名标注。
- 可切换到区域外溢视图，看到周边国家影响点位与摘要。
- 点击伊朗点位可联动选中事件；时间线交互不回归。
- 地图信息在桌面与移动端均可读，无大面积无效空白。
- 测试与构建均通过，不引入新的类型错误。

---

## 2026-02-28 页面自动更新覆盖

### 任务拆解
1. 新增聚合接口 `/api/dashboard`，统一返回页面所需的全部区块数据。
2. 在 `IncidentDashboard` 中建立客户端数据状态，替换初始静态 `data` 直读。
3. 增加自动刷新机制（周期拉取 + 错误降级状态），覆盖时间线、地图、状态卡、声明、核查、来源、社媒、媒体与 FAQ。
4. 确保语言切换时自动刷新仍返回对应语言数据。
5. 增加查询层测试，验证聚合数据结构完整与语言本地化行为。
6. 执行回归验证：`npm test`、`npm run build`。

### 依赖关系
- 2 依赖 1。
- 3 依赖 2。
- 4 依赖 2/3。
- 5 依赖 1。
- 6 依赖全部前置任务。

### 验收标准
- 页面除实时流面板外，其余数据区块可在无需刷新页面情况下自动更新。
- 语言切换后自动刷新请求带 `lang`，并返回对应语言内容。
- 自动刷新失败时页面仍保持最近成功快照可读，不出现崩溃。
- `npm test` 与 `npm run build` 全部通过。

---

## 2026-03-02 高密度 UX 改版（放大消息来源与社媒区）

### 任务拆解
1. 重排 `IncidentDashboard` 首屏网格，提升右侧信息流区域占比，保持现有模块完整不删减。
2. 重构右侧信息流编排：将“可信来源监控”与“社媒来源监控”置于更高优先级区域，并在大屏采用并排双通道。
3. 增强“可信来源监控”列表信息层级（发布方、时间、标题、跳转）与可视高度。
4. 增强 `SocialMediaPanel` 的可读性与可追溯信息密度（平台、账号、发布时间、原帖链接）。
5. 保持语言切换、来源可追溯与现有测试语义不回归。
6. 执行验证：`tests/social-media.test.tsx`、`npm test`、`npm run build`。

### 依赖关系
- 2 依赖 1。
- 3/4 依赖 2。
- 5 依赖 3/4。
- 6 依赖全部前置任务。

### 验收标准
- 首屏右侧“消息来源 + 社媒”可视面积明显提升，桌面端可在同屏显示更多条目。
- 信息密度不下降：其余核心模块（地图、时间线、状态、事实核查等）仍保留且可用。
- 每条来源/社媒信息继续可追溯到原始链接和时间戳。
- `tests/social-media.test.tsx`、`npm test`、`npm run build` 全部通过。

---

## 2026-03-02 数据自动更新链路补齐（全区块）

### 任务拆解
1. 新增来源探测抽象，统一 `HEAD/GET + 版本标记` 解析逻辑。
2. 新增 dashboard 内存快照刷新服务，按 URL 版本变化回写时间字段到各数据区块。
3. 将 `lib/data` 从静态 seed 读取改为刷新服务快照读取，并暴露服务启动入口。
4. 在数据相关 API 路由中启动自动刷新，保证页面打开后后台持续更新。
5. 新增测试覆盖“来源更新 -> 多区块联动更新时间”行为。
6. 执行回归验证：定向测试、全量测试、构建。

### 依赖关系
- 2 依赖 1。
- 3 依赖 2。
- 4 依赖 3。
- 5 依赖 2/3。
- 6 依赖全部前置任务。

### 验收标准
- 当某来源 URL 版本变化时，`sources/social/events/infrastructure/statements/factchecks/regional/media/meta` 相关时间字段可自动联动更新。
- 页面通过 `/api/dashboard` 自动刷新可见新时间戳，不再仅依赖静态 seed 初值。
- 新增测试 `tests/dashboard-refresh.test.ts` 通过。
- `npm test`、`npm run build` 全部通过。

---

## 2026-03-02 线上仅启动消息不刷新（紧急修复）

### 任务拆解
1. 复核 `source-probe` 探测结果，确认 `HEAD/GET`、状态码与版本信号覆盖不足的路径。
2. 为实时流与 dashboard 刷新服务补齐“首次轮询基线比对”，避免首次仅缓存版本不触发更新。
3. 对缺少 `etag/last-modified/dateModified` 的来源增加稳定回退（页面内容指纹）并输出可诊断状态。
4. 增加测试覆盖：首次轮询、版本变化、无匹配来源、探测失败降级。
5. 执行验证：定向测试 + 全量测试 + 构建。
6. 部署并回查线上 API（`/api/live/messages`、`/api/dashboard`）确认有非启动类更新。

### 依赖关系
- 2 依赖 1。
- 3 依赖 1。
- 4 依赖 2/3。
- 5 依赖 4。
- 6 依赖 5。

### 验收标准
- `Live update service started` 之外，`/api/live/messages` 可出现 `source_update` 或来源异常诊断消息。
- `/api/dashboard` 的 `meta.updated_at` 在来源变更后可推进，且相关区块时间字段联动更新。
- 探测失败不会导致接口 500，失败来源会被安全跳过并可追踪。
- `npm test`、`npm run build` 通过后再部署。

---

## 2026-03-03 全站过时过滤 + unknown 后置 + 基础设施证据刷新（一次性交付）

### 任务拆解
1. 在查询层新增统一 72h 新鲜度判定（服务器当前 UTC 基准）。
2. 对 events/infrastructure/statements/sources/social/regional/media 启用强制过时过滤（from/to 不豁免）。
3. 保持 factchecks/faq 不做时间过滤。
4. 在查询层统一基础设施排序：非 unknown 在前，unknown 在后，同组按 last_updated 倒序。
5. 更新 seed 元数据与基础设施证据到 2026-03-03 口径，来源采用 AP + GOV.UK/UN，证据不足保持 unknown。
6. 补齐新增中文文案的英文翻译映射，确保英文界面不回落中文。
7. 增加/更新测试覆盖：72h 过滤、from/to 强制过滤、unknown 后置。
8. 执行回归验证：`npm test`、`npm run build`。

### 依赖关系
- 2/3/4 依赖 1。
- 6 依赖 5。
- 7 依赖 1/2/3/4/5/6。
- 8 依赖全部前置任务。

### 验收标准
- 超过 72h 的数据不会出现在默认 API 与页面展示中（含 from/to 请求）。
- 基础设施卡片展示顺序中 unknown 永远后置。
- 证据不足行业仍可保留 unknown，不会被强制改值。
- `factchecks/faq` 数据不受时间窗口过滤影响。
- 英文页面新增文案均有英文映射，不出现新增中文直出。
- `npm test`、`npm run build` 全部通过。

---

## 2026-03-03 AP Live Updates URL 自动切换适配

### 任务拆解
1. 在来源探测层新增 AP live 页面候选 URL 解析能力（从 HTML 中提取并择优）。
2. 新增 AP live URL 解析接口，支持从当前 AP live 页面解析“最新 live updates URL”。
3. 在 dashboard refresh 流程前置 URL 纠偏：发现新 URL 后，将快照内旧 AP live URL 统一切换到新 URL。
4. 切换 URL 时同步更新相关时间戳与 meta 字段，保证联动刷新链路完整。
5. 增加测试覆盖：解析逻辑、快照 URL 替换、refresh 集成路径。
6. 执行验证：定向测试、全量测试、构建。

### 依赖关系
- 2 依赖 1。
- 3/4 依赖 2。
- 5 依赖 1/2/3/4。
- 6 依赖全部前置任务。

### 验收标准
- AP 导航中的 live updates 链接切换后，系统可在 refresh 中自动切换到新 URL。
- 切换后 `sources/events/infrastructure/...` 中关联的旧 URL 会更新为新 URL。
- 旧 URL 不再作为主追踪目标，后续轮询基于新 URL 生效。
- `npm test` 与 `npm run build` 全部通过。
